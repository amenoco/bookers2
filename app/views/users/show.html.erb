<div class="container px-5 px-sm-0">
  <%= flash[:notice] %>
  <div class="row">
<%= render 'books/book', books: @books %>
 <div class="col-md 8offset-md-1">
      <h2>Users</h2>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th>image</th>
            <th>name</th>
            <th colspan="3"></th>
          </tr>
        </thead>
  <%# @books.each do |book| %>

  <tbody>
    <tr>
      <div class="profile-container">
        <%= image_tag @user.get_profile_image(100,100) %>
        
        <tr>
          <% if @user.id == current_user.id %>
        </tr>
        <tr><%= link_to "Show", books_path %></tr>
        <% end %>
    </tr>
  </tbody>
  </table>
    </div>
  </div>


  <%# end %>
    </div>
  </div>
</div>
</div>
